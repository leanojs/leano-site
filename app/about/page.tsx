import { AboutPage } from "@/components/page/AboutPage";

export const metadata = {
  title: "About — Webpocalypse",
  description:
    "Webpocalypse is a suite of open tools that convert JPG and PNG images to modern formats — WebP and AVIF — fully locally, no uploads.",
};

export default function Page() {
  return <AboutPage />;
}
